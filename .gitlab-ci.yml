image: hosttoday/ht-docker-node:lts

stages:
- release
- trigger

before_script:
  - npm install -g npmci

release:
  stage: release
  script:
    - npmci publish
  only:
    - tags
  tags:
    - docker

trigger:
  stage: trigger
  script:
    - npmci trigger
  only:
    - tags
  tags:
    - docker